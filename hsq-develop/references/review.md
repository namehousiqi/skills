# Code Review

1. 明确审查基线；默认检查 `HEAD` 相对工作区的 staged、unstaged 和 untracked 内容。
2. 先报告按严重度排序的可操作问题，并附文件和行号。
3. 重点检查功能回归、安全和敏感信息、兼容性、并发状态、配置契约、测试隔离和范围混杂。
4. 确认测试真正执行行为而非只匹配源码字符串；检查干净 checkout、被忽略产物和生成文件依赖。
5. 没有发现问题时明确说明，并列出剩余测试缺口或残余风险。
6. 除非用户要求，否则保持只读，不直接修改文件。
